name: wikiGraph
description: Builds the connection graph across all approved pages in a domain. Scans
  bodies for [[wikilinks]], the related_pages frontmatter field, and shared raw_ids
  — writes domains/<d>/indexes/connections.json consumed by the Wiki tab's graph view.
  Deterministic — no LLM call.
schedule: manual
work_scope: all
domain: agents
question: ''
model: ''
include_candidates: false
